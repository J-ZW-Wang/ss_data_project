JSON
- JSON (JaveScript Object Notation) is a data-exchange format commonly used for transmitting data between a server and a client
- Commonly used in web APIs, configuration file and data storage.
- Based on two main structures: objects (similar to Python dictionaries) and arrays (similar to Python lists)
- `json.loads()` to convert strings to JSON object, `json.load()` to convert file to JSON object
- `json.dumps()` to convert JSON object to strings, `json.dump()` to convert JSON object to file
- Print readable JSON string
  ```
    _ = json.loads(raw_CurateOffer['Payload'].iloc[0])
    _ = json.dumps(_, indent=4)
    print(_)
  ```

  